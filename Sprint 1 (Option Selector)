#include <stdio.h>
#include <ctype.h>
#include "option_selector.h"

char getSelection(void) {
    char selection;
    scanf_s(" %c", &selection, 1);

    selection = (char)toupper((unsigned char)selection);
    return selection;
}
